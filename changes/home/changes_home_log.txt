Cambios realizados para integrar el Home web (2025-10-24):

Resumen general:
- Integré tu código web (Home React/JSX) como una página HTML local dentro de la app Android (WebView) y posteriormente migré la pantalla Home a una versión nativa mínima. A continuación detallo cada archivo modificado/añadido, su descripción y el autor de la modificación.

Detalles por archivo (descripción + autor // jere):

1) app/src/main/assets/www/index.html
   - Descripción: Página HTML/JS que contiene la versión web del `Home` (React + Tailwind en runtime para prototipo). Incluye componente `ServiceCard` y lista de `SERVICES`. Usada por `HomeActivity` en WebView como fallback.
   - Autor: // jere

2) app/src/main/java/com/universidad/streamzone/HomeActivity.kt
   - Descripción: Activity que carga `assets/www/index.html` en un `WebView`, añade la interfaz JavaScript `Android` (método `onReserve(serviceJson)`) para comunicación web → Android, y pasa `USER_FULLNAME` como query param. Mantuvimos esta clase como fallback antes de eliminar la WebView.
   - Autor: // jere

3) app/src/main/res/layout/activity_home_webview.xml
   - Descripción: Layout sencillo que contiene un `WebView` empleado por `HomeActivity` para mostrar la versión web del Home.
   - Autor: // jere

4) app/src/main/java/com/universidad/streamzone/HomeNativeActivity.kt
   - Descripción: Nueva Activity nativa para `Home` (Kotlin). Muestra un saludo (TextView `tvGreeting`) y un `RecyclerView` con tarjetas de servicios (`ServiceAdapter`). Acción `onReserve` muestra un `Toast` como placeholder para la acción de compra.
   - Autor: // jere

5) app/src/main/java/com/universidad/streamzone/Service.kt
   - Descripción: Modelo de datos `Service` (data class) que representa id, título, precio y descripción de cada servicio usado por la versión nativa.
   - Autor: // jere

6) app/src/main/java/com/universidad/streamzone/ServiceAdapter.kt
   - Descripción: Adaptador `RecyclerView` que infla `item_service_card.xml` y maneja el click del botón `btnReserve`, invocando la lambda `onReserve(service)` pasada desde la Activity.
   - Autor: // jere

7) app/src/main/res/layout/activity_home_native.xml
   - Descripción: Layout nativo del Home (`HomeNativeActivity`) con `TextView` `tvGreeting` y `RecyclerView` `rvServices`.
   - Autor: // jere

8) app/src/main/res/layout/item_service_card.xml
   - Descripción: Layout de cada tarjeta de servicio (CardView) con `tvServiceTitle`, `tvServicePrice`, `tvServiceDesc` y `btnReserve`. Textos referenciados desde `strings.xml`.
   - Autor: // jere

9) app/src/main/res/values/strings.xml
   - Descripción: Se añadieron entradas nuevas para los textos de las tarjetas (`service_title`, `service_price`, `service_desc`, `btn_buy`) para evitar strings hardcoded y facilitar i18n.
   - Autor: // jere

10) app/src/main/java/com/universidad/streamzone/LoginActivity.kt
    - Descripción: Modificado para que, tras login exitoso, abra `HomeNativeActivity` en vez de `HomeActivity` (WebView). Pasa el extra `USER_FULLNAME` hacia la Activity destino.
    - Autor: // jere

11) app/src/main/java/com/universidad/streamzone/RegisterActivity.kt
    - Descripción: Modificado para que, tras registro exitoso, abra `HomeNativeActivity` y pase `USER_FULLNAME`. También se corrigió la navegación del enlace "Volver al login" para apuntar a `LoginActivity`.
    - Autor: // jere

12) app/src/main/AndroidManifest.xml
    - Descripción: Se registró la nueva `HomeNativeActivity` en el manifiesto. `HomeActivity` (WebView) se conservó como backup (no eliminada aún).
    - Autor: // jere

Registro de acciones y notas técnicas (adicional):
- Se corrigió un fallo de sintaxis accidental en `HomeNativeActivity.kt` (caracteres extra al inicio) durante la creación de la Activity nativa.
  - Autor: // jere
- Se actualizaron referencias a `R.layout` y `R.id` en `ServiceAdapter.kt` para usar la referencia completa (`com.universidad.streamzone.R.*`) y evitar problemas con el analizador estático.
  - Autor: // jere
- Se añadieron recursos de texto en `strings.xml` y se eliminó el uso de cadenas hardcoded en `item_service_card.xml`.
  - Autor: // jere

Estado actual y próximos pasos:
- Estado: La aplicación compila y la versión nativa del Home está integrada y activa tras login/registro. La WebView sigue en el proyecto como fallback; el usuario (tú) puede probar en el emulador antes de eliminarla.
- Próximo paso recomendado: Confirmación del funcionamiento en el emulador. Si confirmas, procedo a eliminar la carpeta `assets/www`, `HomeActivity` y el layout `activity_home_webview.xml`, y añadiré un registro de eliminación en este archivo.

-- Fin del log actualizado (2025-10-24) --
